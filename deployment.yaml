apiVersion: apps/v1
kind: Deployment
metadata:
  name: music-player-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: music-player-app
  template:
    metadata:
      labels:
        app: music-player-app
    spec:
      containers:
        - name: music-player-app
          image: nginx:latest
          ports:
            - containerPort: 80
